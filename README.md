This project is a python http server based on SimpleHttpServer which supports playing videos on browser.

you can run in this command

```bash
python http.py
```

这是基于SimpleHttpServer的改进版本，可以实现对目录的浏览，并且支持在浏览器中在线播放视频。
